import LiveExample from '@/ui/demos/LiveExample';

export const metadata = {
  title: 'signal-tooltip'
};

<div className="prose-signal space-y-6">

# signal-tooltip

<LiveExample tag="signal-tooltip" />

Tooltip / hovercard component.

## Props

- `content: string`
- `variant?: "tooltip" | "hovercard"` (default `"tooltip"`)
- `placement?: "top" | "right" | "bottom" | "left"` (default `"top"`)
- `delay?: number` (default `150`)
- `interactive?: boolean`
- `disabled?: boolean`

## Slots

- default — trigger
- `content` — custom body

</div>
