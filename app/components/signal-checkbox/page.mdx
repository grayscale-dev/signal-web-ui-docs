import LiveExample from '@/ui/demos/LiveExample';

export const metadata = {
  title: 'signal-checkbox'
};

<div className="prose-signal space-y-6">

# signal-checkbox

<LiveExample tag="signal-checkbox" />

Accessible checkbox with indeterminate support.

## Usage
```html
<signal-checkbox label="Subscribe" checked></signal-checkbox>
```

## Props
- `label` string
- `checked` boolean
- `indeterminate` boolean
- `disabled` boolean

## Events
- `valueChange` → `&#123; checked: boolean &#125;`

## Slots
- default: custom label content (optional)

</div>
