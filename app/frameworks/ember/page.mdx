export const metadata = {
  title: 'Ember Wrapper'
};

<div className="prose-signal space-y-6">

# Ember wrapper

Use `@signal-web-ui/ember` to bridge the custom elements into Ember templates.

```hbs
<SignalButton @variant="primary">Launch</SignalButton>
```

## Notes

- Ensure the custom elements are registered once in your app initializer.
- Follow Ember naming conventions for wrapper components.

</div>
